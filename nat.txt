//CLI
exit
ip nat inside source static 'private' 'public'
interface fastEthernet 0/0
ip nat inside
exit
interface fastEthernet 1/0
ip nat inside
exit
interface serial 2/0
ip nat outside
exit


ip route 'public' 'subnet mask' 'serial ip path'
exit